{
  "name": "main",
  "version": "0.1.0",
  "main": ".dist/index.js",
  "author": "Damien ARTIGNAN",
  "license": "MIT",
  "devDependencies": {
    "electron": "^8.2.0"
  },
  "scripts": {
    "electron": "electron --inspect=5858 --remote-debugging-port=9223 dist",
    "start": "set NODE_ENV=dev && nodemon --watch ./src --exec \"tsc && yarn electron\""
  },
  "dependencies": {
    "nodemon": "^2.0.2",
    "typescript": "^3.8.3"
  }
}
